apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: discord-ticket-data
  namespace: discord-ticket
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
