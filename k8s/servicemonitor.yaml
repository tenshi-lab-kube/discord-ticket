apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: discord-ticket
  namespace: discord-ticket
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: discord-ticket
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
