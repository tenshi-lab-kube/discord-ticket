apiVersion: v1
kind: Service
metadata:
  name: discord-ticket
  namespace: discord-ticket
  labels:
    app.kubernetes.io/name: discord-ticket
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: discord-ticket
  ports:
    - name: http
      port: 3000
      targetPort: http
