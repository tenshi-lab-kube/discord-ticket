apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: discord-ticket
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/tenshi-lab-kube/discord-ticket.git
    targetRevision: main
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: discord-ticket
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
